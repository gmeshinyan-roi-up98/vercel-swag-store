export const classes = {
  root: "inline-flex items-center rounded-md border border-border",
  button:
    "inline-flex h-8 w-8 min-w-8 items-center justify-center !px-0 !py-0 hover:bg-muted disabled:opacity-50",
  value: "w-8 text-center text-sm tabular-nums",
} as const;
